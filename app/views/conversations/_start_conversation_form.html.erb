  <%= form_for @message do |f| %>
    <%= hidden_field_tag 'conversation_id', @conversation.id %>
    <%= hidden_field_tag 'offer_id', @offer.id %>
    <%= hidden_field_tag 'receiver_id', @offer.user.id %>
    <%= f.label :content %>
    <%= f.text_area :content %>
    <%= f.submit %>
  <% end %>
